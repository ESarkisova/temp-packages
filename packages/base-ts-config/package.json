{
  "name": "@esarkisova/libs.base-ts-config",
  "version": "1.0.12",
  "license": "ISC",
  "description": "base tsconfig temp packages",
  "files": ["*.json"],
  
  "scripts": {
    "release": "yarn version"
  }
}
